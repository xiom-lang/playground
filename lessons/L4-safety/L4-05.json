{
  "id": "L4-05",
  "title": "Contracts on Methods",
  "level": "L4",
  "duration": "12 min",
  "story": "You've built a BankAccount struct with a balance. But anyone with access to the struct can set balance to -500 — that makes no sense! You want to attach rules to the struct itself: 'You can only withdraw if you have enough money.' With XIOM, you can add `requires:` directly to methods on structs, making the struct guard its own data.",
  "why": "Attaching contracts to struct methods encapsulates validation rules with the data they protect — the struct becomes self-guarding, impossible to put into an invalid state from outside.",
  "narrative": "## Self-Guarding Structs\n\nWhen you add `requires:` to a method on a struct, the method validates the struct's state before executing. The struct protects itself.\n\n### BankAccount with Contract on Withdraw\n\n```xiom\ntype BankAccount = {\n  balance: Float64;\n}\n\nfn BankAccount.withdraw(self, amount: Float64) -> Float64\n  requires: amount > 0.0;\n  requires: amount <= self.balance;\n  ensures: result == self.balance - amount;\n{\n  return self.balance - amount;\n}\n```\n\nThe `self` keyword in the contract refers to the struct instance. The `requires: amount <= self.balance;` means 'the account must have enough money.' This check runs BEFORE the method body.\n\n### Deposit with Contract\n\n```xiom\nfn BankAccount.deposit(self, amount: Float64) -> Float64\n  requires: amount > 0.0;\n  ensures: result == self.balance + amount;\n  ensures: result > self.balance;\n{\n  return self.balance + amount;\n}\n```\n\n### Full Program\n\n```xiom\nfn main() -> Int {\n  let account = BankAccount { balance: 100.0 };\n\n  let bal1 = account.deposit(50.0);\n  io.println(\"After deposit: \" + string.float_to_str(bal1));\n\n  let bal2 = account.withdraw(30.0);\n  io.println(\"After withdraw: \" + string.float_to_str(bal2));\n\n  let bal3 = account.withdraw(200.0);\n  io.println(string.float_to_str(bal3));\n\n  return 0;\n}\n```\n\n### Multiple Methods on One Struct\n\n```xiom\ntype Player = {\n  health: Int;\n  max_health: Int;\n}\n\nfn Player.heal(self, amount: Int) -> Int\n  requires: amount > 0;\n  ensures: result <= self.max_health;\n{\n  let new_hp = self.health + amount;\n  if new_hp > self.max_health { return self.max_health; }\n  else { return new_hp; }\n}\n\nfn Player.take_damage(self, amount: Int) -> Int\n  requires: amount > 0;\n  ensures: result >= 0;\n{\n  let new_hp = self.health - amount;\n  if new_hp < 0 { return 0; }\n  else { return new_hp; }\n}\n```\n\n### Why Methods with Contracts Matter\n\n- **Encapsulation**: The validation lives WITH the data it protects\n- **Reuse**: Every caller of `BankAccount.withdraw` gets validation automatically\n- **Self-documenting**: Reading the method signature tells you all the rules\n- **Impossible misuse**: You can't call `withdraw(200)` on a 100-balance account — the contract stops you",
  "analogy": "A struct method with a contract is like a hotel room safe. The safe (struct) has a keypad (method) with rules: 'You can only open me if you enter the correct code (requires: code is valid).' The safe ENFORCES its own rules — you can't bypass the keypad and just grab the contents. Similarly, `BankAccount.withdraw` enforces its own rules: 'You can only withdraw if you have enough money.' The struct guards itself; callers can't cheat.",
  "code_template": "use xiom.io;\n\ntype BankAccount = {\n  balance: Float64;\n}\n\nfn BankAccount.withdraw(self, amount: Float64) -> Float64\n  // TODO: add requires: amount > 0.0;\n  // TODO: add requires: amount <= self.balance;\n  // TODO: add ensures: result == self.balance - amount;\n{\n  return self.balance - amount;\n}\n\nfn main() -> Int {\n  // TODO: create account with balance 200.0\n  // TODO: withdraw 50.0, print result\n  return 0;\n}\n",
  "solution": "use xiom.io;\n\nuse xiom.string;\nuse xiom.convert;\ntype BankAccount = {\n  balance: Float64;\n}\n\nfn BankAccount.withdraw(self, amount: Float64) -> Float64\n  requires: amount > 0.0;\n  requires: amount <= self.balance;\n  ensures: result == self.balance - amount;\n{\n  return self.balance - amount;\n}\n\nfn main() -> Int {\n  let account = BankAccount { balance: 200.0 };\n  let new_bal = account.withdraw(50.0);\n  io.println(convert.float_to_string(new_bal));\n  return 0;\n}\n",
  "expected_output": "150",
  "try_it": "Add a `deposit` method to BankAccount with `requires: amount > 0.0;` and `ensures: result > self.balance;`. Create an account with 0 balance and deposit 100, then withdraw 40. What happens if you call withdraw(-10)? The amount > 0 requires should trap. Add a `transfer` method that takes another BankAccount and an amount — with requires checking the sender has enough funds. What contracts should `transfer` have? Add `ensures: sender.result == sender.balance - amount;` style ensures (you may need to return a tuple).",
  "concepts": [
    "contracts-on-methods",
    "struct",
    "self",
    "encapsulation",
    "requires",
    "ensures",
    "method-contracts"
  ],
  "tips": [
    "Use `self` in method contracts to reference the struct's fields — `self.balance`, `self.health`",
    "Method contracts run in order: first the caller's code, then the method's requires, then the body, then ensures",
    "Methods with contracts make invalid states unrepresentable — you CAN'T create a situation where balance < 0 through normal method calls"
  ],
  "common_mistakes": [
    "Forgetting that `self` in a method contract refers to the struct BEFORE the method body runs, not after",
    "Writing contracts that depend on mutable state that could change between the requires check and body execution (in multi-threaded contexts)",
    "Expecting `self` to be updated inside ensures — `ensures: self.balance == 0;` checks the ORIGINAL balance, not the returned value"
  ],
  "next_lesson": "L4-06",
  "related": [
    "L4-01",
    "L4-04",
    "L3-37",
    "L2-23"
  ]
}
